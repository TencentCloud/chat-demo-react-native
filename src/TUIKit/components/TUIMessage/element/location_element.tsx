import React from 'react';
import {Text} from 'react-native';
import type {V2TimMessage} from 'react-native-tim-js';

export const LocationElement = (props: {message: V2TimMessage}) => {
  return <Text>["地理位置消息"]</Text>;
};
